source 'https://rubygems.org'

# For localhost development:
gem 'jekyll'

# For Github pages (uncomment to use)
# gem "github-pages", group: :jekyll_plugins

# Used plugins
group :jekyll_plugins do
  gem 'jekyll-paginate'
  gem 'jekyll-paginate-categories'
  gem 'jekyll-regex-replace'
end